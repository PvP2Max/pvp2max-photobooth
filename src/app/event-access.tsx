"use client";

import type { ReactNode } from "react";

// Temporary stub while Firebase auth is being migrated.
export default function EventAccessGate({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
